%div.card-panel
    %span.coral
        %i.material-icons.left cloud_upload
        Subir una nueva categoría

%div.row
    %div
        %form{:action => "", :method => "post"}
            %div.input-field.col.s6
                %input{:id => "name", :type => "text", :name => "nombre", :required => ""}
                %label{:for => "name"} Nombre de la categoría

            %div.input-field.col.s6
                %button{:type => "submit", :name => "sendData", :class => "cyan darken-2 btn-large waves-effect waves-light"} Publicar categoría

- if(isset($_POST['sendData'])){
-    $nombre = $_POST['nombre'];
-    $data->dataCategory($nombre);
-    $data->uploadCategory();
- }